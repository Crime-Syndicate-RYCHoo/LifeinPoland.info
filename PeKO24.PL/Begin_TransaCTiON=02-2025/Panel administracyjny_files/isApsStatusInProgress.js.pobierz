define([], function() {
    /**
     * Funkcja sprawdzająca czy status zasobów jest 'aps:proto' lub 'aps:provisioning'.
     *
     * @param {string} status przekazany status
     *
     * @return {boolean} wynik przyrównania
     */
    return function(status) {
        return status === 'aps:proto' || status === 'aps:provisioning';
    };
});
